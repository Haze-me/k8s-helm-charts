replicaCount: 1

image:
  repository: haze21/api-gateway
  pullPolicy: IfNotPresent
  tag: "4cabc69b"

service:
  type: ClusterIP
  port: 8080

resources:
  requests:
    memory: "256Mi"
    cpu: "200m"
  limits:
    memory: "512Mi"
    cpu: "500m"

secretName: api-gateway-secret